# Group — `deco.group`

<!-- Generated by scripts/gen-node-docs.sh from src/lib/nodes/. Do not edit. -->

| | |
| --- | --- |
| Category | Annotations (`deco`) |
| Canvas name | Group |
| Pin names drawn | yes (compound) |
| Footprint (default params) | 40 × 24 cells = 400 × 240 world units |
| Decoration | yes — enclosure, painted under wires; no pins, not simulated |
| Resizable | `width` 8–400, `height` 4–400 (cells) |

## Node JSON

```json
{
  "id": "n_group1",
  "type": "deco.group",
  "position": {
    "x": 0,
    "y": 0
  },
  "params": {
    "title": "Group",
    "subtitle": "",
    "color": "blue",
    "style": "filled",
    "fontSize": 14,
    "carry": true,
    "width": 40,
    "height": 24
  }
}
```

## Params

| Key | Label | Kind | Allowed | Default | Hint |
| --- | --- | --- | --- | --- | --- |
| `title` | Title | text | ≤ 120 chars | `Group` |  |
| `subtitle` | Subtitle | text | ≤ 200 chars | `` |  |
| `fontSize` | Header size | int | min 8, max 48 | `14` | The title's size; the subtitle follows it. |
| `color` | Colour | color | `gray`, `blue`, `teal`, `green`, `amber`, `red`, `purple`, `pink` | `blue` |  |
| `style` | Style | select | `filled`, `outlined`, `dashed` | `filled` |  |
| `carry` | Move contents | bool | `true` / `false` | `true` | Dragging the group brings what lies inside it. |
| `width` | Width | int | min 8, max 400 | `40` | Grid cells. Or drag a handle on the canvas. |
| `height` | Height | int | min 4, max 400 | `24` |  |

## Pins (default params, rotation 0)

None.

## Behaviour

A labelled box drawn round part of a circuit, to show which gates belong
together — "Adder", "Control", "Stage 2" — with a title, an optional subtitle
and a background colour. It is decoration only: it has no pins and nothing in
the simulation knows it is there.

### Behaviour

A group sits **beneath** the wires and every other element, so it never hides
the circuit it frames. It is picked up by its **header** or its **edge**; a
press anywhere else inside it reaches whatever is there, or starts a rubber
band on empty space, exactly as if the group were not drawn.

With **Move contents** on (the default), dragging the group brings everything
lying wholly inside it along, wires and their bends included. Turn it off to
slide the box on its own. A rubber band only selects a group it completely
surrounds.

### Typical uses

- Framing the stages of a pipeline, or the datapath apart from the control.
- A titled panel round the inputs a user is meant to operate.
- Colour-coding one circuit's blocks in an example or a write-up.
