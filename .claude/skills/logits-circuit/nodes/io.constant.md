# Constant — `io.constant`

<!-- Generated by scripts/gen-node-docs.sh from src/lib/nodes/. Do not edit. -->

| | |
| --- | --- |
| Category | Inputs & outputs (`io`) |
| Canvas name | Constant |
| Pin names drawn | no (basic) |
| Footprint (default params) | 4 × 4 cells = 40 × 40 world units |

## Node JSON

```json
{
  "id": "n_constant1",
  "type": "io.constant",
  "position": {
    "x": 0,
    "y": 0
  },
  "params": {
    "width": 1,
    "value": 1
  }
}
```

## Params

| Key | Label | Kind | Allowed | Default | Hint |
| --- | --- | --- | --- | --- | --- |
| `width` | Bit width | int | min 1, max 64 | `1` |  |
| `value` | Value | int | min 0 | `1` | Decimal. |

## Pins (default params, rotation 0)

`dx, dy` are world units from the node's `position` (its top-left). Pin ids go in wire `from`/`to`.

| Pin id | Name | Dir | Width | Side | dx, dy |
| --- | --- | --- | --- | --- | --- |
| `out` | OUT | out | 1 | right | 40, 20 |

Pins, widths and footprint change with params and rotation. For anything but the defaults run:

```bash
bun .claude/skills/logits-circuit/scripts/pins.ts io.constant '{"width":1,"value":1}' 0
```

## Behaviour

A fixed value, tied onto a net and never changing.

### Behaviour

The digital equivalent of tying a pin to supply or to ground. **Value** is
entered in decimal and driven onto `OUT` least significant bit first, padded
or truncated to **Bit width**.

Nothing about it moves during a run: it emits its value once and holds it, so
it costs nothing per frame.

### Typical uses

- Logic 1 and logic 0 sources — width 1, value 1 or 0.
- A mask for an AND, or an increment operand for `comb.adder`.
- A hard-wired opcode on an ALU's `OP` while testing one operation.
- Tying an unused input to a defined level, so it reads `0` or `1` rather
  than floating at `Z` and poisoning everything downstream with `X`.
