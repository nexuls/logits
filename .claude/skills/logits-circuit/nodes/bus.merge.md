# Merge — `bus.merge`

<!-- Generated by scripts/gen-node-docs.sh from src/lib/nodes/. Do not edit. -->

| | |
| --- | --- |
| Category | Buses (`bus`) |
| Canvas name | Merge |
| Pin names drawn | yes (compound) |
| Footprint (default params) | 8 × 10 cells = 80 × 100 world units |

## Node JSON

```json
{
  "id": "n_merge1",
  "type": "bus.merge",
  "position": {
    "x": 0,
    "y": 0
  },
  "params": {
    "groups": "1,1,1,1"
  }
}
```

## Params

| Key | Label | Kind | Allowed | Default | Hint |
| --- | --- | --- | --- | --- | --- |
| `groups` | Groups | text | ≤ 128 chars | `1,1,1,1` | Lane counts, least significant first — e.g. 4,4 or 1,1,1,1. |

## Pins (default params, rotation 0)

`dx, dy` are world units from the node's `position` (its top-left). Pin ids go in wire `from`/`to`.

| Pin id | Name | Dir | Width | Side | dx, dy |
| --- | --- | --- | --- | --- | --- |
| `in0` | A0 | in | 1 | left | 0, 20 |
| `in1` | A1 | in | 1 | left | 0, 40 |
| `in2` | A2 | in | 1 | left | 0, 60 |
| `in3` | A3 | in | 1 | left | 0, 80 |
| `out` | OUT | out | 4 | right | 80, 50 |

Pins, widths and footprint change with params and rotation. For anything but the defaults run:

```bash
bun .claude/skills/logits-circuit/scripts/pins.ts bus.merge '{"groups":"1,1,1,1"}' 0
```

## Behaviour

Joins narrow lanes back onto one bus, least significant group first.

### Behaviour

**Groups** is a comma-separated list of lane counts, least significant first,
in the same format `bus.split` uses. `4,4` takes two nibbles and produces
an 8-bit bus with `A0` in the low half; `1,1,1,1` gathers four single bits
into a nibble.

The output width is the sum of the groups. Like the split, this is wiring: bits
are placed on the bus exactly as they arrive, with `Z` and `X` carried
through rather than normalised.

### Typical uses

- Assembling a word from individual `io.switch` bits to feed a probe or an
  adder.
- Concatenating an opcode and an operand into an instruction word for
  `mem.rom`.
- Zero- or sign-extending: merge the value with a constant in the high group.
- Round-tripping with `bus.split` to reorder bits — split with one grouping,
  rewire, merge with another.
