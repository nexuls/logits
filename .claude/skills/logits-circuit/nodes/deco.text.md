# Text — `deco.text`

<!-- Generated by scripts/gen-node-docs.sh from src/lib/nodes/. Do not edit. -->

| | |
| --- | --- |
| Category | Annotations (`deco`) |
| Canvas name | Text |
| Pin names drawn | yes (compound) |
| Footprint (default params) | 20 × 8 cells = 200 × 80 world units |
| Decoration | yes — painted above wires; no pins, not simulated |
| Resizable | `width` 2–400, `height` 2–400 (cells) |

## Node JSON

```json
{
  "id": "n_text1",
  "type": "deco.text",
  "position": {
    "x": 0,
    "y": 0
  },
  "params": {
    "text": "## Notes\nDescribe this part of the circuit.",
    "format": "markdown",
    "fontSize": 12,
    "align": "left",
    "color": "default",
    "background": "none",
    "width": 20,
    "height": 8
  }
}
```

## Params

| Key | Label | Kind | Allowed | Default | Hint |
| --- | --- | --- | --- | --- | --- |
| `text` | Text | text | ≤ 10000 chars, multiline | `## Notes Describe this part of the circuit.` |  |
| `format` | Format | select | `markdown`, `plain` | `markdown` |  |
| `fontSize` | Font size | int | min 6, max 96 | `12` |  |
| `align` | Align | select | `left`, `center`, `right` | `left` |  |
| `color` | Colour | color | `default`, `muted`, `gray`, `blue`, `teal`, `green`, `amber`, `red`, `purple`, `pink` | `default` |  |
| `background` | Background | color | `none`, `gray`, `blue`, `teal`, `green`, `amber`, `red`, `purple`, `pink` | `none` |  |
| `width` | Width | int | min 2, max 400 | `20` | Grid cells. Or drag a handle on the canvas. |
| `height` | Height | int | min 2, max 400 | `8` |  |

## Pins (default params, rotation 0)

None.

## Behaviour

Words on the canvas: a heading over a circuit, a note explaining a trick, the
instructions for an example. Plain text or basic Markdown. It is decoration
only — no pins, and the simulation never sees it.

### Behaviour

With **Format** set to Markdown the text understands the everyday subset:
`#` headings, **bold**, *italic*, `inline code`, bulleted and numbered
lists, `>` quotes, links and rules. Raw HTML is shown as
text, not run, and an image as its alt text. **Plain** writes the text exactly as typed, line breaks and
all.

While you type, the box grows taller to fit the words and shrinks back as
you delete — never below the height it had when you started. Text that no
longer fits a box made smaller afterwards is clipped.

### Typical uses

- A title and a one-line description over a board.
- A sticky note: pick a **Background** and it reads as a coloured card.
- Step-by-step instructions beside the switches of an example circuit.
